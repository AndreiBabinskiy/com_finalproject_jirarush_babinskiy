spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: jdbc:h2:mem:jira-test
    username: jira
    password: JiraRush
  liquibase:
    changeLog: "classpath:changelog_h2.sql"